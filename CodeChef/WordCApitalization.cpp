#include <bits/stdc++.h>
using namespace std;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);
	string s;
	cin >> s;
	s[0] = towupper(s[0]);
	cout << s << endl;
	return 0;
}

/*
The towupper() is a built-in function in C/C++ which converts the given wide character into uppercase.
*/